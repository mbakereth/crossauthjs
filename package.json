{
  "name": "crossauth",
  "private": true,
  "version": "0.0.0",
  "description": "Comprehensive package for authentication",
  "scripts": {
    "test": "echo 'No tests defined'",
    "test-all": "pnpm -r run testonce",
    "doc": "typedoc --entryPoints packages/common packages/backend packages/fastify"
  },
  "author": "Matthew Baker",
  "license": "MIT",
  "devDependencies": {
    "typedoc": "^0.25.4"
  }
}
