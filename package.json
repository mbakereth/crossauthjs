{
  "name": "crossauth",
  "private": true,
  "license": "Apache-2.0",
  "version": "0.0.0",
  "description": "Comprehensive package for authentication",
  "scripts": {
    "test": "echo 'No tests defined'",
    "test-all": "pnpm -r run testonce",
    "build-all": "pnpm -r build",
    "doc": "typedoc --entryPoints packages/common packages/backend packages/fastify packages/frontend packages/sveltekit"
  },
  "author": "Matthew Baker",
  "devDependencies": {
    "typedoc": "^0.25.4"
  }
}
