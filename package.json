{
  "name": "crossauth",
  "private": true,
  "version": "0.0.0",
  "description": "Comprehensive package for authentication",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test-all": "pnpm run test",
    "docs": "typedoc",
    "docs-all": "pnpm run docs"
  },
  "author": "Matthew Baker",
  "license": "MIT",
  "devDependencies": {
    "typedoc": "^0.25.4"
  },
  "dependencies": {
    "sveltekitsessionexample": "workspace:^"
  }
}
